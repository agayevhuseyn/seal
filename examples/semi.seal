define factor(n)
	if n == 0 or n == 1 then return 1
	else return n * factor(n - 1)

	return (if n == 0 or n == 1 then 1 else n * factor(n - 1))

	if condition then expr1 else expr2 // ternary

	if condition then stmt1 // shortened if-else
	else stmt2

var a = (1 +
						 2),
		b = 1,
		temp

while true
	temp = a
	a = b
	b = b + temp
